generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           String         @id @default(uuid())
  username     String
  phone        String
  password     String
  email        String         @unique
  created_at   DateTime       @default(now()) @db.Timestamptz()
  ScheduledJob ScheduledJob[]
  sessions     Session[]
}

model ScheduledJob {
  id                 String   @id @default(uuid())
  userId             String
  payload            Json
  AuthorJob          User     @relation(fields: [userId], references: [id])
  run_at             DateTime @db.Timestamptz()
  recurrence_pattern String
  is_recurring       Boolean
  created_at         DateTime @default(now()) @db.Timestamptz()
}

model Outbox {
  id             String    @id @default(uuid())
  aggregate_type String    @default("")
  scheduleId     String
  event_type     String
  payload        Json
  status         String    @default("pending")
  execution_id   String?   @unique
  scheduledAt    DateTime  @default(now()) @db.Timestamptz()
  error_reason   String?
  processed_at   DateTime?
  created_at     DateTime  @default(now()) @db.Timestamptz()
}

model Session {
  id         String   @id @default(uuid())
  userId     String
  AuthorJob  User     @relation(fields: [userId], references: [id])
  token      String   @unique
  expiresAt  DateTime
  created_at DateTime @default(now()) @db.Timestamptz()
}
